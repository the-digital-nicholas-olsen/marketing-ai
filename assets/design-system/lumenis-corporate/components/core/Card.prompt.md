Square-cornered content surfaces and filter chips.

```jsx
<Card>…</Card>
<Card variant="muted">…</Card>        {/* #f2f2f2 — also the product-photo ground */}
<Card variant="floating">…</Card>     {/* soft shadow, e.g. treatment card over a color block */}
<Tag selected>Aesthetics</Tag><Tag>Vision</Tag>
```

No rounded corners, no heavy drop shadows, no colored left-border accents. Never put bullet lists inside boxes on social formats.
