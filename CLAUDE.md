# Claude entry point

Read [AGENTS.md](AGENTS.md). Skills in `.claude/skills/` are generated from `.agents/skills/`; edit only the canonical sources and run `node scripts/sync-skills.mjs`. Verify tools available in this session before operational work.
